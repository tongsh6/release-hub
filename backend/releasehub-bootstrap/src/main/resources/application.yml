spring:
  application:
    name: releasehub
  profiles:
    active: local

management:
  endpoints:
    web:
      exposure:
        include: health,info

releasehub:
  crypto:
    secret-key: ${RELEASEHUB_CRYPTO_SECRET_KEY:}
