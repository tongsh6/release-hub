spring:
  application:
    name: releasehub
  profiles:
    active: local

management:
  endpoints:
    web:
      exposure:
        include: health,info

releasehub:
  crypto:
    # 加密默认关闭。需要加密时在 Profile 中设置 enabled: true 并提供 secret-key
    enabled: false
