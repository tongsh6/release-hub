# Slice: SA-010 解除挂载 UI E2E 复核

- **蓝图归属**：`docs/reports/scenario-acceptance-matrix.md` 七、当前推进队列：SA-010/SA-011 发布计划与风险详情
- **日期**：2026-05-19
- **执行者**：AI
- **状态**：已完成

## 选择理由

- 台账 Top Priority 明确保留解除挂载复核缺口，上一切片只补到前端入口和 Vitest；本切片先收口 UI E2E 子项。
- 该任务能把 SA-010 从“可点击入口”推进到“真实页面用户旅程可回归”，直接减少矩阵缺口。
- 范围窄且风险可控：只改 Playwright E2E 与文档，不改变后端业务行为、数据库结构或 Git Adapter。
- 与项目原则一致：业务数据通过 UI 创建和变更，API 只作为请求成功证据，不用脚本偷造 ReleaseHub 状态。

## 事前约束检查

| 约束 | 状态 | 说明 |
|------|------|------|
| 蓝图归属 | OK | 属于 SA-010 解除挂载 UI E2E 复核缺口 |
| 用户价值 | OK | 发布经理可从窗口详情解除关联迭代，该路径有自动化回归 |
| 端到端路径 | OK | 覆盖 UI 建数、挂载、详情页解除挂载、API 请求成功和页面刷新 |
| 单一目标 | OK | 只补解除挂载 UI E2E，不扩展 GitLab 不可达/权限失败 |
| 可独立验证 | OK | Slice-1 Playwright、typecheck、i18n lint、静态扫描 |
| 可回滚 | OK | 影响集中在 `frontend/e2e/tests/slice-1-group-window.spec.ts` 和文档 |
| 依赖明确 | OK | 依赖既有 `releaseWindowApi.detach`、详情页解除挂载入口和 Slice-1 用户旅程 |
| 风险收敛 | OK | 不新增产品逻辑；抽取 helper 避免关闭窗口用例重复维护挂载流程 |

## 涉及文件

| 文件 | 操作 | 层 |
|------|------|-----|
| `frontend/e2e/tests/slice-1-group-window.spec.ts` | 修改 | E2E Test |
| `docs/reports/scenario-acceptance-matrix.md` | 修改 | Docs |
| `docs/project-ledger.md` | 修改 | Docs |
| `tasks/records/2026-05-19-sa-010-detach-ui-e2e.md` | 新建 | Docs |

## 执行步骤

### Step 1: RED / 缺口确认

- **缺口**：SA-010 只有详情页入口 Vitest，缺少 Playwright 用户旅程复核。
- **目标**：新增 Slice-1 用例覆盖 UI 创建迭代、挂载、详情页解除挂载和页面结果观察。

### Step 2: GREEN

- **测试文件**：`frontend/e2e/tests/slice-1-group-window.spec.ts`
- **实现内容**：
  - 加载 `releaseWindow.noIterations` 和 `common.remove` 文案。
  - 新增 `detach iteration from window detail via UI` 用例。
  - 断言 `POST /api/v1/release-windows/{id}/detach` 成功。
  - 断言解除后关联迭代列表为空、发布计划面板隐藏、DRAFT 窗口仍可继续挂载。
- **验证结果**：首次 Slice-1 运行 11 PASS。

### Step 3: REFACTOR

- 抽取 `createIteration` 和 `attachIterationToWindow` helper。
- 复用到新增解除挂载用例和既有关闭窗口用例，避免同一 UI 建数/挂载流程复制。

### Step 4: VERIFY

| 命令 | 结果 |
|------|------|
| `pnpm run typecheck` | PASS |
| `pnpm i18n:lint` | PASS |
| `pnpm run test:e2e:slice-1` | 11 PASS / 0 FAIL / 0 SKIP |
| `bash scripts/dev/static-scan-topn.sh 10` | PASS |

## 事后检查

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 行为闭环 | OK | 用户可从窗口详情解除挂载，页面刷新为空状态 |
| 层级闭环 | OK | UI 旅程接通既有 API；不新增悬空接口或前端推导业务真相 |
| 测试闭环 | OK | Slice-1 Playwright 覆盖关键路径并回归通过 |
| 架构闭环 | OK | 不改变 DDD 分层；只增强用户旅程验证 |
| 性能闭环 | OK | 测试只在用户确认后等待单次 detach 请求和列表刷新 |
| 文档闭环 | OK | 已同步场景矩阵、项目台账和本执行记录 |
| 工作区闭环 | OK | 已检查 diff、运行静态扫描并保留报告 |

## 静态扫描

- **扫描命令**：`bash scripts/dev/static-scan-topn.sh 10`
- **报告路径**：`.ai/reports/static-scan/20260519-003807/summary.md`
- **TopN 处理结论**：TopN 未发现代码问题；git diff check、backend SpotBugs、frontend ESLint、frontend typecheck 均通过。
- **未解决风险**：无代码扫描风险；SA-011 GitLab 不可达/权限失败类风险、SA-010 带仓库解除挂载真实 GitLab 分支归档复核和更完整发布计划限制仍在矩阵 P1 队列。

## 经验沉淀

- [x] 不新增经验文档。沿用既有前端 E2E 分层与用户旅程建数模式，未产生新的可复用坑点。
